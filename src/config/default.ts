import { Config } from './Config';

export const DEFAULT_CONFIG: Config = {
    scrollDismissPx: 50,
    attributeNames: {
        width: 'data-width',
        height: 'data-height',
        src: 'data-src'
    }
};
